pipeline {
    agent any
    environment {
        CI = 'true'
    }
    stages {
        stage('Build') {
            steps {
                dir("/tmp") {
                  sh "pwd"
              }

        }
        
    }
}
}
