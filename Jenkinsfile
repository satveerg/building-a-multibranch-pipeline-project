pipeline {
    agent any
    environment {
        CI = 'true'
    }
    stages {
        stage('Build') {
            steps {
                dir("/opt") {
                  sh "pwd"
              }

        }
        
    }
}
}
